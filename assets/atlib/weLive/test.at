//import /.at.lang.futures;
import /.at.support.timer;
//def ds := object: {
//def data := [];
//def put(x) {
//data := data + [x];
//};
//};

def abc(x,y){system.println("hihi")};

def dfg(z,a){sytem.println("hehe")};

def brk() {
	def i := 1;
	{|Q|
		while: {true} do: {
			if: (i > 10) then: {
				Q();
			}
			else:{
				system.println(i);
			};
			i := i +1;
		};
	}.escape();
};

def plusser := object: {
	def plus(x,y) {
		//system.println("blub");
		x+y;
	};
};

//def sync := .plus(1,2);
//def async := <-plus(2,3)@FutureMessage;

//def ftr := plusser <+ async;
//whenAny: ftr becomes: { |res|
//	system.println("Resolved with: " + res);
//};

def dur := now() + 2.seconds() - now();
when: dur elapsed: {
	system.println("It works");
}; 

def b := c + d;
def c := 3;
def d := 4;
system.println(b);
