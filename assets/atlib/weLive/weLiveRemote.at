object:{
	def weLiveRemoteInterface := object: {
		
		def parent;
		def init(prnt) {
			parent := prnt;
		};
		def getId(){
			parent.Id;
		};
		
		def getPlayBoard(){
			parent.board;
		};
		def getSessionId(){
			parent.sessionId;
		}
	};
};